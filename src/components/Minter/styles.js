import { makeStyles } from "@material-ui/styles";

const styles = makeStyles(() => ({
    root: {
        color: 'white'
    },
    inputLabel: {
        color: "white",
    }
}));

export default styles;